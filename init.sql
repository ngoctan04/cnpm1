-- Initialize the hotel booking database
-- Tables are created automatically by the backend using SQLAlchemy
-- Sample data is populated by the backend's seed_data.py when the database is empty
-- Ensure the database exists and is ready
SELECT 'Hotel Booking Database initialized successfully' as status; 